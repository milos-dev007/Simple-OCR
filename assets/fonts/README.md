# Fonts

This directory contains open fonts used for synthetic OCR generation.

Expected files:

- `NotoSans-Regular.ttf`
- `NotoSerif-Regular.ttf`
- `RobotoMono-Regular.ttf`

The project uses them to vary glyph shape during rendering while keeping the training data fully synthetic.
